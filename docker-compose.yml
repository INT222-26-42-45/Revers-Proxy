version: '3'
services:
  reverseproxy:
    image: nginx
    restart: always
    container_name: reverseproxy
    volumes: 
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80

networks:
    default:
        external:
            name: skorshoptnetwork
    